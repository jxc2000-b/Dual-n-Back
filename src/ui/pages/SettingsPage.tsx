// SKELETON — bind to UserSettings via storage.loadSettings / saveSettings.
// UI agent: numeric inputs (clamped) for trialsPerSet, trialDurationMs,
// stimulusDurationMs, defaultN. Slider for audioVolume. Toggle for
// showHotkeyButtons. See PLAN.md → "UI / Pages / Settings".

export function SettingsPage() {
  return (
    <div>
      <h2>Settings</h2>
      <p>TODO(ui-agent): numeric inputs + volume slider + reset-to-defaults.</p>
    </div>
  );
}
