// SKELETON — render history table + per-day rollup from storage.listSets() and
// storage/aggregate.rollupByDay. UI agent: add a sparkline (SVG, no deps) for
// avgN-over-time. See PLAN.md → "UI / Pages / Stats".

export function StatsPage() {
  return (
    <div>
      <h2>Stats</h2>
      <p>TODO(ui-agent): list every set, group by day, show max/avg N and accuracy trends.</p>
    </div>
  );
}
